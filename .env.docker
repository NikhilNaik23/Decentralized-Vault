# Docker Environment Variables
# Copy this file to .env when running with docker-compose
# IMPORTANT: Generate new secrets for production!

# JWT Secrets (CHANGE THESE!)
JWT_SECRET=your-jwt-secret-here-generate-a-strong-random-key
JWT_REFRESH_SECRET=your-jwt-refresh-secret-here-generate-a-strong-random-key

# Encryption Keys (CHANGE THESE!)
ENCRYPTION_KEY=your-encryption-key-here-64-hex-characters
VAULT_KEY_SALT=your-vault-key-salt-here-64-hex-characters

# Optional: Ethereum Configuration
ETHEREUM_ENABLED=false
ETHEREUM_NETWORK=sepolia
ETHEREUM_RPC_URL=
ETHEREUM_PRIVATE_KEY=
ETHEREUM_CONTRACT_ADDRESS=

# NOTE: To generate secure keys, run:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"